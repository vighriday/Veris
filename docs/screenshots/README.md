# Screenshots

This directory will hold dashboard screenshots referenced from the main README.

Suggested captures (run `node dist/cli.js .` on a real repo, then screenshot):

- `dashboard-hero.png` — top of the dashboard (Executive Summary band + confidence gauge + Affected Behaviors)
- `workflow-risk-map.png` — graph viz colored by workflow
- `heatmap.png` — workflow heatmap
- `drift-detection.png` — drift card with silent-rewrite flag
- `adversarial-probes.png` — probe list with copy buttons
- `budget-allocator.png` — budget slider + selected targets

Image guidelines: PNG, max 1600px wide, dark background, no personal repo paths in the frame.
